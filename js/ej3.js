//declaramos radio como una constante
const radio = 5;

//realizamos la formula de PI * RADIO ^2
const area = Math.PI * Math.pow(radio, 2);

//mostramos el resultado
console.log(`El area del circulo es ${area}`)

